'use strict';

const _ = require('lodash');

class GrammarManager {
    constructor(modelsPath) {
        this._grammarPath = modelsPath + "grammar.gram";
    }

    addToGrammar(newCommands) {
        // TODO JSGF stuff here
    }
}

module.exports = GrammarManager;
